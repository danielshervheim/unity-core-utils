using UnityEngine;

namespace DSS.CoreUtils.Attributes
{
    // An attribute used to mark variables as read-only in the editor (during runtime).
    public class ReadOnlyRuntimeAttribute : PropertyAttribute { }
}